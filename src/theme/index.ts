// src/theme/index.ts
'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#B7DA3D',
    },
    secondary: {
      main: '#dc004e',
    },
  },
});

export default theme;
